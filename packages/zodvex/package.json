{
  "name": "@packages/zodvex",
  "version": "0.1.0",
  "description": "Zod v4 -> Convex validators mapping with correct optional/nullable semantics.",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "keywords": ["zod", "convex", "validators", "codec", "mapping"],
  "scripts": {
    "type-check": "tsc -p ./tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "convex": "^1.27.0",
    "convex-helpers": ">=0.1.101-alpha.1",
    "zod": "^4.0.0"
  }
}
